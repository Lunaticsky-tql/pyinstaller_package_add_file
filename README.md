# pyinstaller_package_add_file
One program to figure out the way to associate your packaged program to other file

run
```shell
pyinstaller --add-data="[absolute-path-of the file];." -F main.py
```
to test
